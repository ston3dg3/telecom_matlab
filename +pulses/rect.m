function pulse = rect(n_os)
%%RECT Provides the rectangular pulse with oversampling factor n_os

% n_os is the number of pulses that go into the output signal
pulse = ones(1,n_os);

% Funktioniert
end

